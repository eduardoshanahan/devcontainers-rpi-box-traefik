# 2026-01-02

- Added centralized Docker preflight checks (CLI, daemon, Compose v2) and wired into the main playbook.
- Traefik Compose now honors TRAEFIK_LOG_LEVEL instead of forcing DEBUG.
- whoami_host now validates it includes a domain suffix before suffix comparison.
- Traefik role now auto-runs mkcert -install when the local CA is missing.
- Documentation updated to reflect mkcert auto-install and Docker/Compose prerequisites.
- Added optional Traefik dashboard host routing via dynamic config with SAN validation.
- Moved Ubuntu CA installer defaults to scripts/ca-hosts.txt.
- Traefik now auto-replaces target certs signed by a different CA.
- Added global HTTP to HTTPS redirect for Traefik entrypoints.
- Box 03 deployed successfully; HTTPS redirect enabled and dashboard routing configured.
