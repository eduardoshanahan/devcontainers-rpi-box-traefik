# 2025-12-31

- Priority 1: Implement HTTPS per the reference (local CA + wildcard cert), ensure certs are placed under TRAEFIK_CERT_DIR and paths match TRAEFIK_CERT_FILE/TRAEFIK_KEY_FILE.
- Priority 2: Ensure Traefik routes HTTPS correctly (websecure entrypoint, TLS enabled, docker provider exposedByDefault=false, shared web network).
- Priority 3: Validate whoami routing via Host(`whoami.home.arpa`) and confirm DNS points to the Traefik host IP.
- Priority 4: Document trust steps for the local CA on client devices and add a short DNS guidance note (home.arpa).
- Priority 5: Add a template/example for adding new apps via labels and shared network.
